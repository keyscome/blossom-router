# Blossom Router 常见问题

[English](FAQ.md) | [简体中文](FAQ.zh-CN.md)

## Blossom Router 解决什么问题？

当工作负载同时包含日常提示词和真正困难的任务时，它可以减少不必要的高价模型调用。一个 CLI 通过明确命令或确定性规则，把任务映射到可配置的 local、cheap、normal、strong 和 code provider slot。

## 如果我只使用一个模型，它还有价值吗？

通常暂时没有，直接调用 provider 更简单。当你至少使用两个层级、希望跨 provider 保持统一命令，或者需要保护高价额度不被重复任务消耗时，它才更有价值。

## 自动路由会调用另一个模型吗？

不会。它只使用本地关键词和 prompt 长度规则。`bloom auto --dry-run` 不发起 provider 请求，只报告路由与理由。

## 使用 `auto` 必须配置哪些 route？

请配置 `local`、`cheap`、`normal` 和 `strong`，因为当前规则可能选择其中任何一个。`code` 只通过明确命令使用，不会被 `auto` 选择。MVP 在所选 route 缺失时不会静默回退。

## 多个 route 可以使用同一个 provider 吗？

可以。它们可以使用同一厂商的不同模型、不同厂商，或者在验证工作流时临时使用相同 endpoint 和模型。Route 名表示任务意图与成本层级，不绑定具体公司。

## API key 应该放在哪里？

在 YAML 中使用 `api_key_env`，并导出对应环境变量。不要把真实 key 写进会提交的配置文件。公开站点不会询问 key；本地 UI 由服务端读取解析后的配置，也不会把凭据返回浏览器。

## 浏览器 UI 会暴露本地服务吗？

`bloom serve` 只接受 `127.0.0.1` 等 loopback 地址。由于 UI 能调用已配置 provider，它会拒绝 `0.0.0.0`。默认地址是 `http://127.0.0.1:7331`，按 Ctrl+C 停止。

## `code` route 可以接入 Codex 吗？

它可以连接可配置的 OpenAI-compatible chat-completions endpoint。MVP 不假定专有 Codex 协议，也不会加入隐藏 Agent 调用。

## Ollama 应该选择哪个模型？

先使用已经能在 Mac 上稳定运行的模型。当前开发机器已安装 `qwen2.5-coder:14b`，可作为实用的本地编程/通用模型。不同模型的可用性和内存要求会变化，因此该选择始终保持可配置。

## 如何判断路由是否真的省钱？

对一周真实任务做抽样，记录 route、完成时间、答案是否接受，以及是否人工升级。重点观察本地/经济路由占比、升级率、失败率、延迟和估算节省的云端费用。详见[实用性评估指南](docs/evaluation.zh-CN.md)。

## 为什么没有自动重试与回退？

它们可能产生不可见成本，并让路由难以理解。MVP 每次只发起一次可见请求并返回错误。未来若加入重试或回退，必须明确、有界、可配置并可衡量。

## 公开站点可以直接操作我的模型吗？

不可以。公开站点提供文档、适用性评估和配置生成器；真正的本地操作界面请使用 `bloom serve`。
